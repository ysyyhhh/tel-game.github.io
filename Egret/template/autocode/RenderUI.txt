declare namespace game {
    /**
     * AutoCodeEui 生成的界面 
     * euiPath:${shortSkinPath}  
     * made by ${auth}
     * create on ${time} 
    */
    export interface ${shortName}Render {
    
        ${interfaceIds}
        
    }
}