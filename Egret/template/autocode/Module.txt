module game {
    /**
     * AutoCodeEui 生成的界面 
     * euiPath:${shortSkinPath}  
     * made by ${auth}
     * create on ${time} 
    */
	export class ${shortName}Model {
		public constructor() {
			super();
		}
	}

	export let ${shortname}Model: ${shortName}Model;
}